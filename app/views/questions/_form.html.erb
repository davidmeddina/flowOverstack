<%= form_for @question do |f| %>
  
  <%= f.label :title, "Título" %>
  <%= f.text_field :title %>
  
  <%= f.label :content, "Contenido" %>
  <%= f.text_area :content %>
  
  <%= f.submit "Preguntar" %>

  <% end %>